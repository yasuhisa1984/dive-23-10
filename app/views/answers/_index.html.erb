<% answers.each do |answer| %>
  <% unless answer.id.nil? %>
    <div>
      回答者：<%= answer.user.name %>
    </div>
    <div>
      <%= simple_format(answer.content) %>
    </div>
  <% end %>
<% end %>
